package Parsing;

import DataModel.Location;
import java.util.ArrayList;

/**
 * Interface describing the different functionalities for parsing the locations.
 */

public interface Parse {
    ArrayList<Location> parseLocations();
}
